#include <iostream>
using namespace std;
int main(){
    int x;
    cout<< "Enter an integer: ";
    cin>>x;
    if (x>0) {
        cout<<x;

    } else {
        cout<<-x;
    }
    //Another solution for this is; 
    //if (n<0) n= -n
}