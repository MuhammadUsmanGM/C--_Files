#include <iostream>
using namespace std;
int main(){
    int x;
    cout<<"Enter an integer: ";
    cin>>x;
    if (x>99 and x<1000){
        cout<<"Number is three digit";
    }
    else {
        cout<<"Not a three digit number";
    }
    // we can write && in place of and
}